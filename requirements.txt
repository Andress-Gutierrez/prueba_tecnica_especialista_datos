# Dependencias del ETL / DW (Fase 4)
# Requiere Python 3.10+ (recomendado 3.12, alineado con la imagen apache/airflow:3.1.3).
# No incluir Apache Airflow aquí (Airflow corre solo dentro de Docker).

pandas>=2.2,<3
psycopg[binary]>=3.1,<4
pyxlsb>=1.0.10
